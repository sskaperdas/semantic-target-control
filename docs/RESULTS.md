# Results inventory

| File family | Evidence |
|---|---|
| `exp12_*` | quota frontiers and matched baselines |
| `exp13_*` | fixed-pressure comparison |
| `exp14_*` | factual utility and additional scorers |
| `exp16_*` | domain/range/unknown-energy ablation |
| `exp19_*` | query-level bootstrap intervals |
| `exp21_*` | runtime decomposition |
| `exp22_*` | learned LightGBM reranking policies |
| `exp23_*` | relation concentration and macro/weighted utility |
| `exp24_*` | head/tail query slices |
| `window_sensitivity_canonical.csv` | candidate-window robustness |
| `semantic_coverage_candidates.csv` | 26-field semantic-evidence inventory |

All aggregate result files are curated publication artifacts. They are not
substitutes for the licensed/raw datasets, checkpoints or full query-level
outputs.
