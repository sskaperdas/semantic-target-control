# Results index

The `frozen/` directory contains curated publication-level result summaries.
The `provenance/` directory contains the final camera-ready verification.

These files are lightweight evidence artifacts, not full raw experiment outputs.
