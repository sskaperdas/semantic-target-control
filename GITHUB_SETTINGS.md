# Recommended GitHub settings

## Description

> Inference-Time Semantic Target Control for knowledge graph completion rankings from frozen scorers (ISWC 2026).

## Topics

`knowledge-graph-completion`, `knowledge-graphs`, `semantic-web`, `ontology`, `link-prediction`, `constrained-ranking`, `inference-time-control`, `reproducibility`, `pykeen`, `shacl`

## Social preview

Upload `assets/stc-social-preview.png`.

## Repository settings

- Default branch: `main`
- Enable Issues and Discussions only if they will be monitored.
- Enable private vulnerability reporting.
- Keep GitHub Actions enabled for the bundled CI workflow.
- Require the `research-artifact-checks` workflow before merging into `main` when branch protection is enabled.

## Suggested release

`v1.0.3-iswc2026`
